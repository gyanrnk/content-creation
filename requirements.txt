# ── Streamlit Community Cloud (review_app.py) ke liye — JAAN-BOOJH ke LEAN ──────
# Streamlit Cloud repo-root ka requirements.txt install karta hai. Review app ko
# sirf itna chahiye. Pehle yahan pura video-pipeline (moviepy/imageio-ffmpeg/
# google-api/numpy) tha -> Cloud deploy bahut slow + error de raha tha.
#
# PURA pipeline (moviepy, edge-tts, google-api, ...) => requirements-build.txt
#   GitHub Actions wahi install karta hai (workflow me set hai).
#   Local dev:  env\Scripts\python.exe -m pip install -r requirements-build.txt
streamlit>=1.30
requests==2.32.5
